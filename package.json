{
  "name": "dialogtool",
  "version": "1.0.0",
  "description": "A browser-based dialog editor using React Flow. It loads dialog data from `data/dialog.json` and allows you to visually edit nodes and choices. Select a node to edit speaker, text, and choices in the sidebar. Add new nodes or download the dialog as JSON.",
  "main": "src/main.jsx",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0",
  "type": "module",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "reactflow": "11.11.4"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
